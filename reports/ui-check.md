# UI check — конструктор инвойса

## Дата и метод

- **Дата:** 2026-08-27 (UTC+3), прогон Playwright ~13:27
- **Инструмент:** Playwright (Chromium, headless), изолированный browser context (чистый origin/storage)
- **URL:** `http://127.0.0.1:5199`
- **localStorage:** ключ `invoice-constructor.v1`
- **Замечание по порту:** `localhost:5173`–`5176` были заняты процессами, слушающими только `[::1]`; из среды проверки IPv6 недоступен (`EACCES`/`ECONNREFUSED`). Для проверки поднят Vite с `--host 127.0.0.1 --port 5199 --strictPort`.

## Затронутые поверхности

- Форма создания инвойса
- Карточка инвойса
- Список с фильтром статусов (tabs: все / ожидают / оплачены / истекли)
- Экран оплаты `?pay=`

## Сценарии

| Сценарий                                     | Результат | Заметка                                                     |
| -------------------------------------------- | --------- | ----------------------------------------------------------- |
| Пустой список                                | ok        | текст «Пока пусто» виден                                    |
| Поверхность: форма создания                  | ok        | amount/currency/description/ttl×3/сабмит на месте           |
| Отказ: пустая сумма                          | ok        | кнопка «Выставить счёт» disabled                            |
| Отказ: сумма 0                               | ok        | alert: «Сумма должна быть больше нуля»                      |
| Отказ: отрицательная сумма                   | ok        | alert: «Сумма должна быть числом больше нуля»               |
| Отказ: дабл-клик по сабмиту                  | ok        | создан 1 инвойс, requestKey уникален                        |
| Состояние: pending (после создания)          | ok        | карточка `data-status=pending`, штамп «ожидает»             |
| Happy path: копировать ссылку                | ok        | clipboard содержит `pay=<id>`                               |
| Экран `?pay=` (открытие)                     | ok        | INV-title + кнопка «Оплатить (демо)»                        |
| Happy path: «Оплатить (демо)» на `?pay=`     | ok        | статус `paid`, штамп «оплачен»                              |
| Happy path: «Отметить оплаченным» (карточка) | ok        | статус `paid` в storage и UI                                |
| Состояние: paid                              | ok        | подтверждено после оплаты с карточки                        |
| Список: фильтр статусов                      | ok        | все=3, ожидают=1, оплачены=1, истекли=1                     |
| Состояние: expired                           | ok        | `expiresAt` в прошлом через localStorage + reload → «истёк» |

## Что сломалось

Ничего. Все проверенные сценарии — **ok**.

## Баги

Нет.

## Что осталось / ограничения

- Проверка шла не на `:5173`, а на `127.0.0.1:5199` (см. замечание по порту выше).
- Clipboard в headless сработал после `grantPermissions(['clipboard-read','clipboard-write'])`; без этого в headless API часто недоступен — стоит иметь в виду при повторных прогонах.
- Визуальный/регрессионный скриншот-дифф не делался — только функциональные сценарии и состояние DOM/storage.
- Исходники приложения не менялись; временная папка `temp-ui-check` удаляется после отчёта.
